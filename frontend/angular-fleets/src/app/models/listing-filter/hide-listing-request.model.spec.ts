import { HideListingRequest } from './hide-listing-request.model';

describe('HideListingRequest', () => {
  it('should create an instance', () => {
    expect(new HideListingRequest(1)).toBeTruthy();
  });
});
