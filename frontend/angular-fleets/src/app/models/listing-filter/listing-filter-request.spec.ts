import { ListingFilterRequest } from './listing-filter-request';

describe('ListingFilter', () => {
  it('should create an instance', () => {
    expect(new ListingFilterRequest()).toBeTruthy();
  });
});
