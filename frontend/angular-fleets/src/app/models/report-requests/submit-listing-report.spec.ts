import { SubmitListingReport } from './submit-listing-report';

describe('SubmitListingReport', () => {
  it('should create an instance', () => {
    expect(new SubmitListingReport()).toBeTruthy();
  });
});
