import { CreateListingRequest } from './create-listing-request';

describe('CreateListingModel', () => {
  it('should create an instance', () => {
    expect(new CreateListingRequest()).toBeTruthy();
  });
});
