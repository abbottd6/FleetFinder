import { UnmuteAndProvisionRequest } from './unmute-and-provision-request';

describe('UnmuteAndProvisionRequest', () => {
  it('should create an instance', () => {
    expect(new UnmuteAndProvisionRequest({} as any, {} as any)).toBeTruthy();
  });
});
