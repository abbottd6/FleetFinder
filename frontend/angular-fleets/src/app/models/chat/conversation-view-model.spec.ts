import { ConversationViewModel } from './conversation-view-model';

describe('ConversationViewModel', () => {
  it('should create an instance', () => {
    expect(new ConversationViewModel()).toBeTruthy();
  });
});
